version: "3.9"

services:
    app:
      build: .
      environment:
        - REDIS_HOST=redis
      ports:
            - "5000:5000"
      depends_on:
        - redis

    redis:
        image: redis:7
